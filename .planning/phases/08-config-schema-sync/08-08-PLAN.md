---
phase: 08-config-schema-sync
plan: 08
type: execute
wave: 5
depends_on: ["08-03", "08-05", "08-06", "08-07"]
files_modified:
  - docs/appsettings-Production.md
  - docs/IIS-Setup.md
  - UPGRADING.md
  - CHANGELOG.md
autonomous: true
requirements: [STAB-007, STAB-008, STAB-009, STAB-010, STAB-011, STAB-012]
must_haves:
  truths:
    - "docs/appsettings-Production.md absorbs all comment content stripped from the template (STAB-007)"
    - "docs/appsettings-Production.md documents the schema location, validation behavior, and Event Log troubleshooting"
    - "docs/IIS-Setup.md notes the PowerShell 7+ requirement and Event Log source registration"
    - "UPGRADING.md documents -ConfigSync modes (Merge/Review/None) and behavior on upgrade"
    - "UPGRADING.md documents how to diagnose failed startup validation (Event Viewer, source PassReset, event ID 1001)"
    - "CHANGELOG.md [Unreleased] under v1.4.0 records STAB-007..012 entries"
  artifacts:
    - path: "docs/appsettings-Production.md"
      provides: "Operator config reference (absorbs former template comments + schema/validation guidance)"
      contains: "appsettings.schema.json"
    - path: "docs/IIS-Setup.md"
      provides: "PowerShell 7 requirement note + Event Log source guidance"
      contains: "PowerShell 7"
    - path: "UPGRADING.md"
      provides: "ConfigSync modes documented + Event Log troubleshooting"
      contains: "-ConfigSync"
    - path: "CHANGELOG.md"
      provides: "v1.4.0 entries for STAB-007..012"
      contains: "STAB-007"
  key_links:
    - from: "docs/appsettings-Production.md"
      to: "src/PassReset.Web/appsettings.schema.json"
      via: "operator-facing reference to schema location + validation rules"
      pattern: "appsettings\\.schema\\.json"
    - from: "UPGRADING.md"
      to: "Install-PassReset.ps1 -ConfigSync"
      via: "operator-facing guidance on upgrade behavior"
      pattern: "-ConfigSync"
---

<objective>
Update operator-facing documentation to reflect Phase 8 behavior changes: new schema, runtime validation, `-ConfigSync` modes, Event Log troubleshooting path, PowerShell 7+ requirement. Record CHANGELOG entries for STAB-007..012.

Purpose: Phase 8 changes operator workflows (new param, new failure surface, new schema artifact). Without docs, operators discover behavior by trial and error.
Output: 4 doc files updated. No code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-config-schema-sync/08-CONTEXT.md
@.planning/phases/08-config-schema-sync/08-PATTERNS.md
@.planning/phases/08-config-schema-sync/08-01-SUMMARY.md
@.planning/phases/08-config-schema-sync/08-03-SUMMARY.md
@.planning/phases/08-config-schema-sync/08-04-SUMMARY.md
@.planning/phases/08-config-schema-sync/08-05-SUMMARY.md
@.planning/phases/08-config-schema-sync/08-06-SUMMARY.md
@.planning/phases/08-config-schema-sync/08-07-SUMMARY.md
@docs/appsettings-Production.md
@docs/IIS-Setup.md
@UPGRADING.md
@CHANGELOG.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update docs/appsettings-Production.md (schema + absorbed comments + Event Log)</name>
  <files>docs/appsettings-Production.md</files>
  <read_first>
    - docs/appsettings-Production.md (full current content)
    - .planning/phases/08-config-schema-sync/08-01-SUMMARY.md (decisions on schema sections)
    - .planning/phases/08-config-schema-sync/08-03-SUMMARY.md (validators + Event Log helper)
    - src/PassReset.Web/appsettings.schema.json
    - src/PassReset.Web/appsettings.Production.template.json (post 08-01)
  </read_first>
  <behavior>
    - Contains a section "## Authoritative schema" pointing to `src/PassReset.Web/appsettings.schema.json` and explaining D-01..D-04 (Draft 2020-12, structural+types+enums only, custom `x-passreset-obsolete` marker).
    - Contains "## Section notes (formerly inline in template)" with verbatim text of every `//` comment block stripped in plan 08-01 (verify presence; add if missing).
    - Contains "## Startup validation" describing fail-fast, the OptionsValidationException 502 outcome, and the Event Log troubleshooting path (Event Viewer, Application log, source `PassReset`, event ID 1001).
    - Contains "## Validation rules per options class" listing each of the 7 options classes and key validation invariants from plan 08-03 validators.
    - Existing content (key reference, examples) is preserved; new content is additive.
  </behavior>
  <action>
    1. Open `docs/appsettings-Production.md`. Read existing structure (TOC, section order).
    2. Verify section `## Section notes (formerly inline in template)` exists from plan 08-01. If missing, recover the comment text from git history (`git show HEAD~N:src/PassReset.Web/appsettings.Production.template.json`) and add the section now.
    3. Insert near the top (after intro/TOC) a new `## Authoritative schema` section. Content must mention:
       - Path: `src/PassReset.Web/appsettings.schema.json`
       - Format: JSON Schema Draft 2020-12
       - Validation scope: required keys, types (boolean/integer/string/array), enums, patterns, numeric ranges
       - Cross-field rules enforced at runtime by `IValidateOptions<T>` (not in schema, per D-04)
       - Custom markers: `x-passreset-obsolete: true` and `x-passreset-obsolete-since: "1.3.2"`
       - Pre-flight: `Test-Json -Path appsettings.Production.json -SchemaFile appsettings.schema.json` runs on every upgrade BEFORE syncing; failure halts install with actionable error
       - PowerShell 7+ required on the install host
    4. Insert a `## Startup validation` section. Content must include:
       - Each options class is validated via `AddOptions<T>().Bind(...).ValidateOnStart()`
       - Failure throws `OptionsValidationException` at DI build; IIS returns HTTP 502
       - Before re-throwing, host writes failures to Windows Application Event Log under source `PassReset`, event ID 1001, type Error
       - Diagnosing 502: open Event Viewer, Windows Logs, Application; filter source `PassReset` event ID 1001
       - Sample error message in D-08 format: `PasswordChangeOptions.LdapPort: must be integer between 1 and 65535 (got "0"). Edit appsettings.Production.json or run Install-PassReset.ps1 -Reconfigure.`
       - Edit live config (default `C:\inetpub\PassReset\appsettings.Production.json`), then `Restart-WebAppPool -Name PassResetPool`
       - If no Event Log entry appears: source not registered. Re-run `Install-PassReset.ps1` (registers source idempotently in prerequisites step).
    5. Insert a `## Validation rules per options class` section as a Markdown table covering all 7 classes:
       - `PasswordChangeOptions`: LdapHostnames non-empty when UseAutomaticContext=false; LdapPort 1..65535
       - `WebSettings`: UseDebugProvider=true requires Development environment (enforced inline in Program.cs)
       - `SmtpSettings`: when Host set: Port 1..65535, FromAddress contains `@`; Username/Password both empty or both set
       - `SiemSettings`: when Syslog.Enabled: Host non-empty + Port 1..65535 + Protocol Udp/Tcp; AlertOnEvents entries valid SiemEventType
       - `EmailNotificationSettings`: see validator
       - `PasswordExpiryNotificationSettings`: when Enabled: DaysBeforeExpiry >= 1, NotificationTimeUtc matches HH:MM, PassResetUrl starts with https://
       - `ClientSettings`: when Recaptcha.Enabled: SiteKey + PrivateKey non-empty; MinimumScore 0..1
       Add a closing note: "Validation failures NEVER echo secret values; Password, PrivateKey, and similar fields surface as `(got \"<redacted>\")`."
    6. Save and check Markdown lints if a markdownlint config exists in the repo.
  </action>
  <verify>
    <automated>pwsh -Command "$content = Get-Content 'docs/appsettings-Production.md' -Raw; $required = @('appsettings.schema.json', 'Draft 2020-12', 'IValidateOptions', 'Event Log', 'event ID 1001', 'PowerShell 7', 'x-passreset-obsolete'); $missing = $required | Where-Object { $content -notmatch [Regex]::Escape($_) }; if ($missing) { Write-Error ('Missing required references: ' + ($missing -join ', ')); exit 1 }; exit 0"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '## Authoritative schema' docs/appsettings-Production.md` returns 1
    - `grep -c '## Startup validation' docs/appsettings-Production.md` returns 1
    - `grep -c '## Validation rules per options class' docs/appsettings-Production.md` returns 1
    - `grep -c '## Section notes (formerly inline in template)' docs/appsettings-Production.md` returns 1
    - `grep -c 'event ID 1001' docs/appsettings-Production.md` returns >= 1
    - `grep -c '<redacted>' docs/appsettings-Production.md` returns >= 1
    - `grep -c 'appsettings.schema.json' docs/appsettings-Production.md` returns >= 2
  </acceptance_criteria>
  <done>Operator-facing config doc covers schema, validation behavior, troubleshooting, and absorbed comment content.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update docs/IIS-Setup.md (PowerShell 7 requirement + Event Log source)</name>
  <files>docs/IIS-Setup.md</files>
  <read_first>
    - docs/IIS-Setup.md (full current content; locate prerequisites or system-requirements section)
    - .planning/phases/08-config-schema-sync/08-04-SUMMARY.md (PowerShell 7 + Event Log decisions)
  </read_first>
  <behavior>
    - Contains a "## PowerShell 7 requirement" subsection (or equivalent) noting the installer requires pwsh 7+ and linking to Microsoft's installer.
    - Contains a note about Event Log source `PassReset` being registered automatically by the installer on first run; if registration fails, startup validation messages won't appear in Event Viewer.
  </behavior>
  <action>
    1. Open `docs/IIS-Setup.md`. Locate the prerequisites section (likely near the top — covers IIS, .NET hosting bundle).
    2. Add a new subsection or augment existing prerequisites with:
       ```markdown
       ### PowerShell 7 (required for Install-PassReset.ps1)

       Install-PassReset.ps1 uses `Test-Json -SchemaFile` (Phase 8 schema validation), which is only
       available in PowerShell 7+. Windows PowerShell 5.1 is NOT supported.

       Install PowerShell 7 from the Microsoft Store or via:
       ```powershell
       winget install Microsoft.PowerShell
       ```

       Run the installer from `pwsh.exe`, not `powershell.exe`:
       ```powershell
       pwsh -ExecutionPolicy Bypass -File .\Install-PassReset.ps1
       ```

       ### Windows Event Log source

       The installer registers Windows Event Log source `PassReset` (under the Application log) during
       prerequisites. This source is used by the ASP.NET Core host to surface configuration validation
       failures (event ID 1001).

       If registration fails (e.g., due to UAC interruption), startup validation errors will still cause
       the host to throw, but the failure will not appear in Event Viewer. Re-run the installer to retry
       registration.
       ```
    3. If the existing doc has a "Quick install" or sample command, update it to use `pwsh` instead of `powershell` if needed.
  </action>
  <verify>
    <automated>pwsh -Command "$content = Get-Content 'docs/IIS-Setup.md' -Raw; $required = @('PowerShell 7', 'pwsh', 'Event Log source', 'PassReset', 'event ID 1001'); $missing = $required | Where-Object { $content -notmatch [Regex]::Escape($_) }; if ($missing) { Write-Error ('Missing required references: ' + ($missing -join ', ')); exit 1 }; exit 0"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'PowerShell 7' docs/IIS-Setup.md` returns >= 1
    - `grep -c 'pwsh' docs/IIS-Setup.md` returns >= 1
    - `grep -c 'Event Log source' docs/IIS-Setup.md` returns >= 1
    - `grep -c 'event ID 1001' docs/IIS-Setup.md` returns >= 1
  </acceptance_criteria>
  <done>Operators preparing a host know they need pwsh 7 and that Event Log source is auto-registered.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update UPGRADING.md (-ConfigSync modes + Event Log troubleshooting) and CHANGELOG.md</name>
  <files>UPGRADING.md, CHANGELOG.md</files>
  <read_first>
    - UPGRADING.md (full current content; locate the section listing prior upgrade procedures)
    - CHANGELOG.md (locate `[Unreleased]` section under v1.4.0, or create one if absent)
    - .planning/phases/08-config-schema-sync/08-04-SUMMARY.md, 08-05-SUMMARY.md, 08-06-SUMMARY.md (sync mode behavior recap)
  </read_first>
  <behavior>
    - UPGRADING.md gains a new section "## v1.4.0 — Configuration schema and sync" covering: schema artifact is new, pre-flight validation added, `-ConfigSync <Merge|Review|None>` parameter, default behavior on upgrade (interactive prompt or Merge under -Force), how to diagnose 502 after upgrade (Event Viewer, source `PassReset`).
    - CHANGELOG.md `[Unreleased]` (or v1.4.0 in-progress) section gains entries for STAB-007..012 under appropriate categories (Added / Changed / Fixed).
  </behavior>
  <action>
    1. **UPGRADING.md**: append (or insert in version-order) a new section:
       ```markdown
       ## v1.4.0 — Configuration schema and sync

       Phase 8 introduces a JSON Schema (`appsettings.schema.json`) that governs every valid key in
       `appsettings.Production.json`. The installer now validates your live configuration on every upgrade
       and can sync new keys from the schema's defaults without overwriting your customizations.

       ### Breaking changes

       - **PowerShell 7+ required.** `Install-PassReset.ps1` now uses `Test-Json -SchemaFile`, which is unavailable
         in Windows PowerShell 5.1. Install PowerShell 7 (`winget install Microsoft.PowerShell`) and invoke the
         installer via `pwsh -File .\Install-PassReset.ps1`.

       - **`appsettings.Production.template.json` is now pure JSON.** Inline `//` comments have been removed.
         All operator-facing notes have moved to [`docs/appsettings-Production.md`](docs/appsettings-Production.md).

       ### New parameter: `-ConfigSync <Merge|Review|None>`

       Controls how the installer reconciles your live `appsettings.Production.json` with the schema
       on upgrade.

       | Mode | Behavior |
       |------|----------|
       | `Merge` (default on upgrade) | Adds missing keys using schema defaults. NEVER modifies existing values. NEVER merges array contents (arrays are atomic). Reports obsolete keys without removing them. |
       | `Review` | Prompts per-key for each missing key (default Yes) and each obsolete key (default No). |
       | `None` | Skips sync entirely. Drift check still reports differences but takes no action. |

       When `-ConfigSync` is omitted:
       - Interactive upgrade: installer prompts `Config sync: [M]erge additions / [R]eview each / [S]kip? [M]`
       - With `-Force`: defaults to `Merge`
       - Fresh install: resolves to `None` (template is copied fresh; no live config to sync)

       ### New pre-flight validation

       Before syncing, the installer runs:
       ```powershell
       Test-Json -Path appsettings.Production.json -SchemaFile appsettings.schema.json
       ```
       Failure halts the install with the offending field path and reason. Edit the live config to fix
       the field, then re-run the installer.

       ### Diagnosing 502 after upgrade

       The ASP.NET Core host now fails fast on misconfigured options. If you see HTTP 502 after upgrade:

       1. Open Event Viewer, Windows Logs, Application.
       2. Filter by source **`PassReset`** event ID **1001**.
       3. Read the failure messages (format: `Section.Field: reason (got "value"). Edit appsettings.Production.json or run Install-PassReset.ps1 -Reconfigure.`).
       4. Edit `C:\inetpub\PassReset\appsettings.Production.json`, then `Restart-WebAppPool -Name PassResetPool`.

       If no Event Log entry appears, the source may not be registered. Re-run `Install-PassReset.ps1`
       to retry registration.
       ```
    2. **CHANGELOG.md**: under `[Unreleased]` (or the v1.4.0 in-progress section, depending on existing convention), add:
       ```markdown
       ### Added
       - **STAB-008** (gh#27): Authoritative `appsettings.schema.json` (JSON Schema Draft 2020-12) defining every valid configuration key, type, and default. Ships with every release.
       - **STAB-009** (gh#25): Pre-flight configuration validation at install time (`Test-Json`) and at host startup (`IValidateOptions<T>` + `ValidateOnStart()`). CI validates the template against the schema on every push.
       - **STAB-010** (gh#24): Schema-driven additive-merge sync on upgrade — adds missing keys from schema defaults; never modifies operator overrides; treats arrays atomically.
       - **STAB-011** (gh#26): New `-ConfigSync <Merge|Review|None>` installer parameter with interactive prompt resolution. Defaults to `Merge` under `-Force`.
       - Windows Event Log source `PassReset` (event ID 1001) for startup validation failures. Registered by `Install-PassReset.ps1`.

       ### Changed
       - **STAB-007** (gh#22): `appsettings.Production.template.json` is now pure JSON. Inline comments stripped; content relocated to `docs/appsettings-Production.md`.
       - **STAB-012** (gh#37): Schema-drift check rewritten to consume the schema as source of truth. Always runs on upgrade; no longer silently skips when the live config parses successfully.
       - `Install-PassReset.ps1` now requires PowerShell 7+ (`Test-Json -SchemaFile` is unavailable in Windows PowerShell 5.1). Document `pwsh -File .\Install-PassReset.ps1` as the supported invocation.
       ```
       Format-match the existing CHANGELOG entries (Keep a Changelog convention).
  </action>
  <verify>
    <automated>pwsh -Command "$ok = $true; $u = Get-Content 'UPGRADING.md' -Raw; $uReq = @('-ConfigSync', 'Merge', 'Review', 'None', 'event ID 1001', 'PowerShell 7', 'Test-Json'); $uMissing = $uReq | Where-Object { $u -notmatch [Regex]::Escape($_) }; if ($uMissing) { Write-Error ('UPGRADING missing: ' + ($uMissing -join ', ')); $ok = $false }; $c = Get-Content 'CHANGELOG.md' -Raw; $cReq = @('STAB-007', 'STAB-008', 'STAB-009', 'STAB-010', 'STAB-011', 'STAB-012', 'appsettings.schema.json', '-ConfigSync'); $cMissing = $cReq | Where-Object { $c -notmatch [Regex]::Escape($_) }; if ($cMissing) { Write-Error ('CHANGELOG missing: ' + ($cMissing -join ', ')); $ok = $false }; if (-not $ok) { exit 1 }; exit 0"</automated>
  </verify>
  <acceptance_criteria>
    - UPGRADING.md: `grep -c '\-ConfigSync' UPGRADING.md` returns >= 3 (param header + Merge/Review/None refs)
    - UPGRADING.md: `grep -c 'event ID 1001' UPGRADING.md` returns >= 1
    - UPGRADING.md: `grep -c 'PowerShell 7' UPGRADING.md` returns >= 1
    - UPGRADING.md: `grep -c 'Test-Json' UPGRADING.md` returns >= 1
    - CHANGELOG.md: each of `STAB-007`, `STAB-008`, `STAB-009`, `STAB-010`, `STAB-011`, `STAB-012` appears at least once: `for k in STAB-007 STAB-008 STAB-009 STAB-010 STAB-011 STAB-012; do grep -c "$k" CHANGELOG.md; done` returns 6 lines each >= 1
    - CHANGELOG.md: `grep -c 'appsettings.schema.json' CHANGELOG.md` returns >= 1
  </acceptance_criteria>
  <done>Upgrade path documented; release notes record every Phase 8 STAB requirement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → operator | Documentation guides operator decisions; misleading docs cause misconfiguration. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-29 | Information disclosure | Doc examples leak secrets | mitigate | Examples in `docs/appsettings-Production.md` use placeholder values (e.g., `LdapPort: 0` for invalid example, never real credentials). Acceptance criteria asserts `<redacted>` placeholder convention. |
| T-08-30 | Tampering | Outdated docs after future schema changes | accept | Documentation maintenance is ongoing; CHANGELOG entries for future schema changes are required by repo convention (CLAUDE.md commit/release process). Out-of-scope for Phase 8 to enforce automatically. |
</threat_model>

<verification>
1. docs/appsettings-Production.md gains all four required new sections
2. docs/IIS-Setup.md notes pwsh 7 + Event Log source
3. UPGRADING.md documents -ConfigSync modes + 502 diagnosis
4. CHANGELOG.md records all six STAB requirements
</verification>

<success_criteria>
Plan 08-08 complete when:
- All four docs updated with required content
- All STAB-007..012 referenced in CHANGELOG
- Operators upgrading from v1.3.x have a clear runbook
</success_criteria>

<output>
After completion, create `.planning/phases/08-config-schema-sync/08-08-SUMMARY.md` with:
- Sections added per file
- Cross-doc consistency check (no contradictions in -ConfigSync default behavior across docs)
- Files modified
</output>
