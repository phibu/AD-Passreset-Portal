---
phase: 08-config-schema-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/PassReset.Web/appsettings.Production.template.json
  - src/PassReset.Web/appsettings.schema.json
  - src/PassReset.Web/PassReset.Web.csproj
autonomous: true
requirements: [STAB-007, STAB-008]
must_haves:
  truths:
    - "appsettings.Production.template.json is pure JSON with zero comment lines"
    - "appsettings.schema.json exists at repo root of Web project as authoritative key manifest"
    - "Schema validates the template (Test-Json passes locally)"
    - "Both files ship in publish output (Content include in csproj)"
  artifacts:
    - path: "src/PassReset.Web/appsettings.Production.template.json"
      provides: "Pure-JSON production template"
      contains: '"WebSettings"'
    - path: "src/PassReset.Web/appsettings.schema.json"
      provides: "JSON Schema Draft 2020-12 manifest of every valid key + default + obsolete marker"
      contains: '"$schema": "https://json-schema.org/draft/2020-12/schema"'
  key_links:
    - from: "src/PassReset.Web/appsettings.schema.json"
      to: "src/PassReset.Web/appsettings.Production.template.json"
      via: "Test-Json structural conformance"
      pattern: "template MUST validate against schema"
    - from: "src/PassReset.Web/PassReset.Web.csproj"
      to: "schema + template"
      via: "<Content Include CopyToOutputDirectory='PreserveNewest' />"
      pattern: "ships in publish/ output"
---

<objective>
Establish the JSON foundation for Phase 8: strip every `//` comment from `appsettings.Production.template.json` (D-15) and create the authoritative `appsettings.schema.json` (JSON Schema Draft 2020-12) that defines every valid key, type, default, and obsolete marker (D-01..D-04, D-11).

Purpose: Every downstream Phase 8 plan (validators, installer pre-flight, installer sync, drift check, CI step, publish packaging) depends on these two artifacts existing and validating against each other.
Output: Pure-JSON template + authoritative schema, both shipping in publish output.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-config-schema-sync/08-CONTEXT.md
@.planning/phases/08-config-schema-sync/08-PATTERNS.md
@src/PassReset.Web/appsettings.Production.template.json
@src/PassReset.Web/appsettings.json
@src/PassReset.Web/Models/WebSettings.cs
@src/PassReset.Web/Models/ClientSettings.cs
@src/PassReset.Web/Models/SmtpSettings.cs
@src/PassReset.Web/Models/SiemSettings.cs
@src/PassReset.Web/Models/EmailNotificationSettings.cs
@src/PassReset.Web/Models/PasswordExpiryNotificationSettings.cs
@src/PassReset.PasswordProvider/PasswordChangeOptions.cs

<interfaces>
<!-- Schema constraint set per D-04: type, required, enum, pattern, minimum/maximum, default. -->
<!-- AVOID: if/then/else, oneOf/anyOf, format (PowerShell Test-Json doesn't enforce format). -->
<!-- Custom extensions: x-passreset-obsolete: true and x-passreset-obsolete-since: "1.3.2" per D-11. -->

JSON Schema Draft 2020-12 root template:
```json
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://passreset.local/schemas/appsettings.schema.json",
  "title": "PassReset appsettings.Production.json",
  "type": "object",
  "additionalProperties": true,
  "required": [...],
  "properties": { ... }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Strip all // comments from appsettings.Production.template.json (D-15)</name>
  <files>src/PassReset.Web/appsettings.Production.template.json</files>
  <read_first>
    - src/PassReset.Web/appsettings.Production.template.json (current JSONC content — comments at top header, around Logging section ~lines 2-6 and 171-174, plus any inline section dividers)
    - .planning/phases/08-config-schema-sync/08-CONTEXT.md (D-15 — pure JSON, no comments)
    - docs/appsettings-Production.md (target for relocated comment content)
  </read_first>
  <behavior>
    - File parses successfully via `pwsh -Command "Get-Content 'src/PassReset.Web/appsettings.Production.template.json' -Raw | ConvertFrom-Json"` with no errors.
    - File parses successfully via `dotnet run` JSON parser (no `JsonReaderException`).
    - All operator-facing comment content is preserved in `docs/appsettings-Production.md` (no information loss).
  </behavior>
  <action>
    1. Open `src/PassReset.Web/appsettings.Production.template.json`. Identify every line beginning (after optional whitespace) with `//` or containing inline `//` comments (PATTERNS.md notes blocks at ~lines 2-6 header and ~lines 171-174 around Logging).
    2. Extract the textual content of each comment block VERBATIM (preserve wording — operators rely on these as inline docs).
    3. Append the extracted comment content to `docs/appsettings-Production.md` under a new section `## Section notes (formerly inline in template)`. Group by the JSON section the comment annotated (e.g., "### Logging", "### Branding", "### Header"). Use prose form: "By default logs go to %SystemDrive%\\inetpub\\logs\\PassReset\\passreset-YYYYMMDD.log. Override "path" in WriteTo[File].Args to change location."
    4. Delete the comment lines from the template. Do NOT change any actual JSON value, key name, or structure.
    5. Verify the result is valid JSON: `pwsh -Command "Get-Content 'src/PassReset.Web/appsettings.Production.template.json' -Raw | ConvertFrom-Json | Out-Null"` exits 0.
    6. Verify zero `//` lines remain: `grep -c '^\s*//' src/PassReset.Web/appsettings.Production.template.json` returns 0.
    7. **Do NOT modify `src/PassReset.Web/appsettings.json`** — it is the dev-default; it stays JSONC. Only the production template becomes pure JSON.
  </action>
  <verify>
    <automated>pwsh -Command "Get-Content 'src/PassReset.Web/appsettings.Production.template.json' -Raw | ConvertFrom-Json | Out-Null; if ($LASTEXITCODE -ne 0) { exit 1 }; $count = (Select-String -Path 'src/PassReset.Web/appsettings.Production.template.json' -Pattern '^\s*//').Count; if ($count -gt 0) { Write-Error 'Comments still present'; exit 1 }; exit 0"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^\s*//' src/PassReset.Web/appsettings.Production.template.json` returns `0`
    - `pwsh -Command "Get-Content 'src/PassReset.Web/appsettings.Production.template.json' -Raw | ConvertFrom-Json"` exits 0
    - Top-level keys preserved: `WebSettings`, `PasswordChangeOptions`, `SmtpSettings`, `SiemSettings`, `ClientSettings`, `EmailNotificationSettings`, `PasswordExpiryNotificationSettings`, `Serilog` (whichever subset existed pre-edit) — verify count matches pre-edit count via `pwsh -Command "(Get-Content 'src/PassReset.Web/appsettings.Production.template.json' -Raw | ConvertFrom-Json).PSObject.Properties.Name.Count"`
    - `docs/appsettings-Production.md` contains a new section `## Section notes (formerly inline in template)` with the extracted comment text
  </acceptance_criteria>
  <done>Template is pure JSON, parses cleanly, comment content preserved in operator docs.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create appsettings.schema.json (JSON Schema Draft 2020-12) covering every options class</name>
  <files>src/PassReset.Web/appsettings.schema.json</files>
  <read_first>
    - src/PassReset.Web/appsettings.Production.template.json (post-Task-1, structural source — every section becomes a properties entry)
    - src/PassReset.Web/Models/WebSettings.cs (key list + types)
    - src/PassReset.Web/Models/ClientSettings.cs (key list + types + nested objects)
    - src/PassReset.Web/Models/SmtpSettings.cs
    - src/PassReset.Web/Models/SiemSettings.cs (Syslog, AlertEmail nested + AlertOnEvents enum array)
    - src/PassReset.Web/Models/EmailNotificationSettings.cs
    - src/PassReset.Web/Models/PasswordExpiryNotificationSettings.cs
    - src/PassReset.PasswordProvider/PasswordChangeOptions.cs (full options class — AllowedUsernameAttributes, RestrictedAdGroups, AllowedAdGroups, NotificationEmailStrategy enum, etc.)
    - .planning/phases/08-config-schema-sync/08-CONTEXT.md (D-01..D-04, D-11 — keyword set, obsolete marker convention)
    - .planning/phases/08-config-schema-sync/08-PATTERNS.md ("`appsettings.schema.json` (NEW)" section — concrete example shape)
  </read_first>
  <behavior>
    - Schema parses as valid JSON.
    - Schema validates the post-Task-1 template via `Test-Json -Path src/PassReset.Web/appsettings.Production.template.json -SchemaFile src/PassReset.Web/appsettings.schema.json` returning `$true`.
    - Every top-level options section in the template is represented as a `properties` entry in the schema with at least `type: object`, `required: [...]` (for must-exist children), and `properties: {...}` for known keys.
    - All defaults documented in Models/*.cs default-value initializers appear as `default:` entries in the schema.
    - Schema uses ONLY: `type`, `required`, `enum`, `pattern`, `minimum`, `maximum`, `default`, `properties`, `items`, `additionalProperties`, plus custom `x-passreset-obsolete` / `x-passreset-obsolete-since`. NO `if`/`then`/`else`/`oneOf`/`anyOf`/`format`.
  </behavior>
  <action>
    1. Create `src/PassReset.Web/appsettings.schema.json` with the JSON Schema Draft 2020-12 root:
       ```json
       {
         "$schema": "https://json-schema.org/draft/2020-12/schema",
         "$id": "https://passreset.local/schemas/appsettings.schema.json",
         "title": "PassReset appsettings.Production.json",
         "type": "object",
         "additionalProperties": true,
         "required": ["WebSettings", "PasswordChangeOptions", "SmtpSettings", "SiemSettings", "ClientSettings"],
         "properties": { /* sections below */ }
       }
       ```
    2. For each top-level section present in `appsettings.Production.template.json`, add a `properties.{Section}` entry:
       - `WebSettings` (boolean: `EnableHttpsRedirect` default `true`, `UseDebugProvider` default `false`)
       - `PasswordChangeOptions` (per `PasswordChangeOptions.cs`: `UseAutomaticContext` boolean, `LdapHostnames` array of string, `LdapPort` integer 1..65535 default 636, `BaseDn` string, `LdapUsername` string, `LdapPassword` string, `AllowedUsernameAttributes` array of string with enum elements `["samaccountname","userprincipalname","mail"]`, `RestrictedAdGroups` array of string, `AllowedAdGroups` array of string, `PortalLockoutThreshold` integer minimum 1 default 3, `PortalLockoutWindow` string pattern `^\\d{2}:\\d{2}:\\d{2}$` default `"00:30:00"`, `NotificationEmailStrategy` enum `["Mail","UserPrincipalName","SamAccountNameAtDomain","Custom"]` default `"Mail"`, `FailOpenOnPwnedCheckUnavailable` boolean default `false`, `AllowSetPasswordFallback` boolean default `false`, plus any other present-in-Models keys)
       - `SmtpSettings` (`Host`, `Port` integer 1..65535, `Username`, `Password`, `UseStartTls` boolean, `FromAddress` string, `FromName` string)
       - `SiemSettings` with nested `Syslog` (`Enabled` boolean default false, `Host` string, `Port` integer 1..65535 default 514, `Protocol` enum `["Udp","Tcp"]` default `"Udp"`) and nested `AlertEmail` (`Enabled` boolean, `Recipients` array of string, `AlertOnEvents` array of string with enum elements matching `SiemEventType` values: `["PasswordChanged","InvalidCredentials","UserNotFound","PortalLockout","ApproachingLockout","RateLimitExceeded","RecaptchaFailed","ChangeNotPermitted","ValidationFailed","Generic"]`)
       - `ClientSettings` (mirror every property in `ClientSettings.cs` — labels, validation flags, recaptcha config, etc. Nested `Recaptcha` object with `Enabled` boolean, `SiteKey` string, `MinimumScore` number minimum 0 maximum 1)
       - `EmailNotificationSettings` (per Models/*.cs)
       - `PasswordExpiryNotificationSettings` (`Enabled` boolean, `DaysBeforeExpiry` integer minimum 1, `NotificationTimeUtc` string pattern `^\\d{2}:\\d{2}$`, `PassResetUrl` string)
       - `Serilog` (loose schema: `type: object`, `additionalProperties: true` — Serilog config is open-ended; just require its presence)
    3. For each section, set `required: [...]` listing keys that MUST exist (use the C# property defaults / required-by-runtime as guide; conservative on `required` — only mark a key required if absence breaks the app).
    4. Add `additionalProperties: true` at every nested level so operator-added unknown keys don't fail validation (schema is for known-key drift detection, not lockdown).
    5. **Obsolete-key example (D-11):** if any current key is known-obsolete (e.g., consult template + Models), add to its property: `"x-passreset-obsolete": true, "x-passreset-obsolete-since": "1.3.2"`. If no current obsolete keys exist, document the convention via a top-level `"$comment"` field: `"$comment": "Custom extensions: x-passreset-obsolete (boolean), x-passreset-obsolete-since (string SemVer). Used by Install-PassReset.ps1 sync."`
    6. Validate locally:
       ```pwsh
       Test-Json -Path src/PassReset.Web/appsettings.Production.template.json -SchemaFile src/PassReset.Web/appsettings.schema.json
       ```
       Must return `$true`. If `$false`, fix the schema (NOT the template — D-02 makes the schema authoritative for keys, but Task 1's template is the structural baseline).
    7. **Constraint check:** `grep -E 'if|then|else|oneOf|anyOf|"format"' src/PassReset.Web/appsettings.schema.json` MUST return zero matches (D-04 keyword-set restriction).
  </action>
  <verify>
    <automated>pwsh -Command "$valid = Test-Json -Path 'src/PassReset.Web/appsettings.Production.template.json' -SchemaFile 'src/PassReset.Web/appsettings.schema.json' -ErrorAction Stop; if (-not $valid) { exit 1 }; $forbidden = (Select-String -Path 'src/PassReset.Web/appsettings.schema.json' -Pattern '\"if\"|\"then\"|\"else\"|\"oneOf\"|\"anyOf\"|\"format\"').Count; if ($forbidden -gt 0) { Write-Error 'Forbidden keywords present'; exit 1 }; exit 0"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/PassReset.Web/appsettings.schema.json` exists
    - First non-whitespace content matches: `grep -c '"$schema": "https://json-schema.org/draft/2020-12/schema"' src/PassReset.Web/appsettings.schema.json` returns `1`
    - Schema is valid JSON: `pwsh -Command "Get-Content 'src/PassReset.Web/appsettings.schema.json' -Raw | ConvertFrom-Json"` exits 0
    - Schema validates the template: `pwsh -Command "Test-Json -Path 'src/PassReset.Web/appsettings.Production.template.json' -SchemaFile 'src/PassReset.Web/appsettings.schema.json'"` outputs `True`
    - Every top-level options section name appears in schema's `properties`: for each of `WebSettings`, `PasswordChangeOptions`, `SmtpSettings`, `SiemSettings`, `ClientSettings`, `EmailNotificationSettings`, `PasswordExpiryNotificationSettings`, `grep -c '"{Section}":' src/PassReset.Web/appsettings.schema.json` returns >= 1
    - Forbidden keywords absent: `grep -cE '"(if|then|else|oneOf|anyOf|format)":' src/PassReset.Web/appsettings.schema.json` returns `0`
    - At least one explicit `default` per scalar section (e.g., `grep -c '"default":' src/PassReset.Web/appsettings.schema.json` returns >= 10)
  </acceptance_criteria>
  <done>Schema is the authoritative manifest, validates the template, uses only PowerShell-Test-Json-compatible keywords.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Wire schema + template into csproj Content include (ship in publish output)</name>
  <files>src/PassReset.Web/PassReset.Web.csproj</files>
  <read_first>
    - src/PassReset.Web/PassReset.Web.csproj (current Content includes — confirm `appsettings.Production.template.json` is already there)
    - .planning/phases/08-config-schema-sync/08-PATTERNS.md ("Integration Points" — schema must ship in publish output)
  </read_first>
  <behavior>
    - `dotnet publish -c Release src/PassReset.Web/PassReset.Web.csproj -o /tmp/passreset-publish-test` produces `appsettings.schema.json` in the output root (alongside `appsettings.Production.template.json`).
    - `dotnet build` succeeds with no warnings about missing files.
  </behavior>
  <action>
    1. Open `src/PassReset.Web/PassReset.Web.csproj`. Locate the existing `<ItemGroup>` containing `<Content Include="appsettings.Production.template.json" ...>` (or equivalent).
    2. If `appsettings.Production.template.json` is already in the csproj as a `<Content>` item with `CopyToOutputDirectory="PreserveNewest"`, add a sibling for the schema:
       ```xml
       <Content Include="appsettings.schema.json">
         <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
         <CopyToPublishDirectory>PreserveNewest</CopyToPublishDirectory>
       </Content>
       ```
    3. If `appsettings.Production.template.json` is currently included via SDK glob (no explicit entry), add BOTH explicit entries (template + schema) using the same pattern shown above to guarantee `CopyToPublishDirectory` is set (default web SDK only handles `appsettings.json`/`appsettings.{Env}.json`).
    4. Run `dotnet build src/PassReset.sln --configuration Release` to confirm no errors.
    5. Run a publish dry-run: `dotnet publish src/PassReset.Web/PassReset.Web.csproj -c Release -o /tmp/passreset-publish-test` and verify both files land in `/tmp/passreset-publish-test/`.
  </action>
  <verify>
    <automated>dotnet build src/PassReset.sln --configuration Release && dotnet publish src/PassReset.Web/PassReset.Web.csproj -c Release -o /tmp/passreset-publish-test && test -f /tmp/passreset-publish-test/appsettings.schema.json && test -f /tmp/passreset-publish-test/appsettings.Production.template.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'appsettings.schema.json' src/PassReset.Web/PassReset.Web.csproj` returns >= 1
    - `dotnet build src/PassReset.sln --configuration Release` exits 0
    - After publish: `/tmp/passreset-publish-test/appsettings.schema.json` exists and matches `src/PassReset.Web/appsettings.schema.json` byte-for-byte
    - After publish: `/tmp/passreset-publish-test/appsettings.Production.template.json` exists
  </acceptance_criteria>
  <done>Schema and template both ship in publish output; downstream installer can read them from `$PhysicalPath`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → publish output | Schema integrity must survive build/package. Tampering at this boundary weakens validation. |
| publish output → installer | Installer reads schema from publish output to drive sync + drift check. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | appsettings.schema.json (supply chain) | mitigate | Schema is checked into git; any modification surfaces in commit history + PR review. CI in plan 08-02 validates template-against-schema on every push, so a weakened schema that no longer covers the template fails CI. |
| T-08-02 | Information disclosure | appsettings.schema.json | accept | Schema describes key names + types but contains no secret values. Defaults in schema are the same defaults already in `appsettings.json` (public). |
| T-08-03 | Denial of service | template parsing post-comment-strip | mitigate | Task 1 verification commands prove the file parses via both PowerShell and the .NET JSON parser before commit. |
</threat_model>

<verification>
1. `pwsh -Command "Test-Json -Path 'src/PassReset.Web/appsettings.Production.template.json' -SchemaFile 'src/PassReset.Web/appsettings.schema.json'"` outputs `True`
2. Template has zero `//` comments
3. Schema uses only D-04-allowed keywords
4. Both files copy to publish output via csproj Content include
5. `dotnet build` is green
</verification>

<success_criteria>
Plan 08-01 complete when:
- Template is valid JSON (STAB-007 ✓)
- Schema is the single source of truth, validates the template (STAB-008 ✓ partially — runtime + installer wiring in later plans)
- Both ship in release zip
- Downstream plans (02 CI, 03 validators, 04..06 installer, 07 publish, 08 docs) have the artifacts they depend on
</success_criteria>

<output>
After completion, create `.planning/phases/08-config-schema-sync/08-01-SUMMARY.md` with:
- Decisions made (e.g., obsolete-key example used vs convention-only `$comment`)
- Schema sections covered + any sections deliberately left loose (`additionalProperties: true`)
- Defaults captured per section (audit table)
- File paths created/modified
- Lessons / follow-ups
</output>
