---
phase: 09-security-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/PassReset.Tests/Web/Controllers/RateLimitAndRecaptchaTests.cs
autonomous: true
requirements: [STAB-014]
tags: [security, rate-limiting, recaptcha, integration-tests]
must_haves:
  truths:
    - "6th POST to /api/password from the same test factory within a 5-minute window returns HTTP 429 (TooManyRequests)."
    - "reCAPTCHA enabled + syntactically-valid-but-semantically-invalid token returns ApiErrorCode.InvalidCaptcha."
    - "reCAPTCHA disabled (Enabled=false OR PrivateKey empty) accepts a request with empty Recaptcha field."
    - "Each test uses a fresh per-instance WebApplicationFactory subclass — rate-limit partition state does not leak across tests."
  artifacts:
    - path: src/PassReset.Tests/Web/Controllers/RateLimitAndRecaptchaTests.cs
      provides: "4 integration tests (rate-limit enforce, rate-limit OK, recaptcha invalid, recaptcha disabled)"
      min_lines: 150
  key_links:
    - from: "RateLimitAndRecaptchaTests"
      to: "WebApplicationFactory<Program>"
      via: "per-test factory instances with scenario-specific AddInMemoryCollection overrides"
      pattern: "WebApplicationFactory<Program>"
    - from: "RateLimitAndRecaptchaTests.Recaptcha_EnabledWithInvalidToken"
      to: "https://www.google.com/recaptcha/api/siteverify"
      via: "_recaptchaHttp (live network call — D-19)"
      pattern: "InvalidCaptcha"
---

<objective>
STAB-014 — Integration-test coverage for rate-limit + reCAPTCHA enforcement on `POST /api/password`. Four scenarios per D-07 using per-test `WebApplicationFactory<Program>` subclasses. reCAPTCHA enabled path hits real Google siteverify with deliberately invalid token (D-19, no abstraction this phase).

Purpose: Prove security middleware fires correctly in both enabled and disabled configurations — no regressions in fixed-window rate limit or reCAPTCHA branch.
Output: New `RateLimitAndRecaptchaTests.cs` with 4 `[Fact]` tests. No production code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-security-hardening/09-CONTEXT.md
@.planning/phases/09-security-hardening/09-RESEARCH.md
@.planning/phases/09-security-hardening/09-PATTERNS.md
@.planning/phases/09-security-hardening/09-VALIDATION.md
@src/PassReset.Tests/Web/Controllers/PasswordControllerTests.cs
@src/PassReset.Web/Controllers/PasswordController.cs
@src/PassReset.Web/Program.cs

<interfaces>
From src/PassReset.Web/Program.cs — existing rate-limit policy (do not modify):
```csharp
// Named policy "password-fixed-window": 5 requests / 5 min per IP; reject with 429
builder.Services.AddRateLimiter(options =>
{
    options.AddPolicy("password-fixed-window", httpContext => /* per-IP partition */);
    options.OnRejected = async (context, _) => { /* emits SIEM RateLimitExceeded */ };
});
```

From src/PassReset.Web/Controllers/PasswordController.cs:
```csharp
[HttpPost]
[EnableRateLimiting("password-fixed-window")]
[RequestSizeLimit(8192)]
public async Task<IActionResult> PostAsync([FromBody] ChangePasswordModel model)
```

From PasswordControllerTests.cs — REUSABLE patterns (copy inline or share via a helper class):
- `DebugFactory : WebApplicationFactory<Program>` (lines 66-89)
- `ApiResultDto` / `ApiErrorItemDto` (lines 42-64)
- `MakeRequest(string username)` (lines 31-38)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create RateLimitAndRecaptchaTests with 2 rate-limit scenarios</name>
  <files>src/PassReset.Tests/Web/Controllers/RateLimitAndRecaptchaTests.cs</files>
  <read_first>
    - src/PassReset.Tests/Web/Controllers/PasswordControllerTests.cs (entire file — copy DebugFactory, ApiResultDto, MakeRequest patterns)
    - src/PassReset.Web/Program.cs (understand the rate-limit policy registration + OnRejected handler — do NOT modify)
    - .planning/phases/09-security-hardening/09-PATTERNS.md §"Fixture pattern — per-test factory isolation"
    - .planning/phases/09-security-hardening/09-RESEARCH.md §"Pattern 2: Rate-limiter partition isolation"
    - .planning/phases/09-security-hardening/09-RESEARCH.md §"Pitfall 1: Rate-limiter partition leak across tests"
  </read_first>
  <behavior>
    - Test: `RateLimit_SixthRequestInWindow_Returns429` — first 5 POSTs succeed (200 OK), 6th returns 429 TooManyRequests. Uses a dedicated `RateLimitFactory` instance so partition starts clean.
    - Test: `RateLimit_FirstRequestInFreshFactory_Succeeds` — fresh factory + single POST returns 200 (isolation regression guard — ensures per-test factory creates a new rate-limit partition per D-08).
  </behavior>
  <action>
    Create `src/PassReset.Tests/Web/Controllers/RateLimitAndRecaptchaTests.cs`:

    1. File-scoped namespace `PassReset.Tests.Web.Controllers;`
    2. Usings: `System.Net`, `System.Net.Http.Json`, `System.Text.Json.Serialization`, `Microsoft.AspNetCore.Hosting`, `Microsoft.AspNetCore.Mvc.Testing`, `Microsoft.Extensions.Configuration`, `PassReset.Common`, `PassReset.Web.Models`.
    3. `public class RateLimitAndRecaptchaTests`.
    4. Nested sealed factory subclass `public sealed class RateLimitFactory : WebApplicationFactory<Program>` — copy the full `AddInMemoryCollection` block from PasswordControllerTests.DebugFactory (lines 74-85). Keys: UseDebugProvider=true, EnableHttpsRedirect=false, Recaptcha:Enabled=false, SiemSettings:Syslog:Enabled=false, SiemSettings:AlertEmail:Enabled=false, MinimumDistance=0, EmailNotificationSettings:Enabled=false, PasswordExpiryNotificationSettings:Enabled=false, PortalLockoutThreshold=0, UseAutomaticContext=true. `builder.UseEnvironment("Development");`
    5. Inline wire DTOs (`ApiResultDto`, `ApiErrorItemDto`) with `[JsonPropertyName]` — copy from PasswordControllerTests.cs lines 42-64.
    6. `MakeRequest(string username)` helper — copy from PasswordControllerTests.cs lines 31-38.
    7. `[Fact] public async Task RateLimit_SixthRequestInWindow_Returns429()`:
       - `using var factory = new RateLimitFactory();`
       - `using var client = factory.CreateClient(new WebApplicationFactoryClientOptions { AllowAutoRedirect = false });`
       - Loop `for (int i = 0; i < 5; i++)` submit POST with any passing username (e.g., `"alice"` which is not in the debug-provider magic list → success). Each response expected 200 OK — `Assert.Equal(HttpStatusCode.OK, r.StatusCode)`.
       - 6th POST — `Assert.Equal(HttpStatusCode.TooManyRequests, r.StatusCode)`.
    8. `[Fact] public async Task RateLimit_FirstRequestInFreshFactory_Succeeds()`:
       - Construct a NEW `RateLimitFactory` (not the one from test 1) to prove partition isolation.
       - Submit ONE POST; assert 200 OK.
    9. No `IClassFixture` — per-test factory per Pitfall 1 (09-RESEARCH.md).
    10. Do NOT modify production code.
  </action>
  <verify>
    <automated>dotnet test src/PassReset.sln --configuration Release --filter "FullyQualifiedName~RateLimitAndRecaptchaTests.RateLimit" --nologo</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f src/PassReset.Tests/Web/Controllers/RateLimitAndRecaptchaTests.cs`
    - `grep -c "RateLimit_SixthRequestInWindow_Returns429" src/PassReset.Tests/Web/Controllers/RateLimitAndRecaptchaTests.cs` == 1
    - `grep -c "RateLimit_FirstRequestInFreshFactory_Succeeds" src/PassReset.Tests/Web/Controllers/RateLimitAndRecaptchaTests.cs` == 1
    - `grep -n "HttpStatusCode.TooManyRequests" src/PassReset.Tests/Web/Controllers/RateLimitAndRecaptchaTests.cs` returns at least 1 match
    - `grep -n "new RateLimitFactory" src/PassReset.Tests/Web/Controllers/RateLimitAndRecaptchaTests.cs` returns at least 2 matches (two tests, two factories)
    - `grep -n "for (int i = 0; i < 5" src/PassReset.Tests/Web/Controllers/RateLimitAndRecaptchaTests.cs` returns at least 1 match (the 5-request warmup loop)
    - Tests green: `dotnet test --filter "FullyQualifiedName~RateLimitAndRecaptchaTests.RateLimit"` exits 0
  </acceptance_criteria>
  <done>Two rate-limit tests green; 6th request in window returns 429; isolation regression guard passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add reCAPTCHA enabled + disabled scenarios to RateLimitAndRecaptchaTests</name>
  <files>src/PassReset.Tests/Web/Controllers/RateLimitAndRecaptchaTests.cs</files>
  <read_first>
    - src/PassReset.Tests/Web/Controllers/RateLimitAndRecaptchaTests.cs (file from Task 1 — Edit, do not overwrite)
    - src/PassReset.Web/Controllers/PasswordController.cs §reCAPTCHA branch (ValidateRecaptchaAsync path + the `_recaptchaHttp` static call)
    - .planning/phases/09-security-hardening/09-RESEARCH.md §"Pitfall 4: reCAPTCHA verify endpoint unreachable from CI"
    - .planning/phases/09-security-hardening/09-CONTEXT.md §"D-19" (post-research locked decision — live siteverify with invalid token)
  </read_first>
  <behavior>
    - Test: `Recaptcha_EnabledWithInvalidToken_ReturnsInvalidCaptcha` — With `Recaptcha:Enabled=true` and `Recaptcha:PrivateKey="6LeIxAcTAAAAAGG-vFI1TnRWxMZNFuojJ4WifJWe"` (Google's test documented invalid-secret) + a random non-empty token like `"ThisTokenWillNotVerify"`, POST returns 400 with `errors[0].ErrorCode == ApiErrorCode.InvalidCaptcha` (6). Network call to `https://www.google.com/recaptcha/api/siteverify` is expected — D-19.
    - Test: `Recaptcha_DisabledWithEmptyToken_RequestProceeds` — With `Recaptcha:Enabled=false` (RateLimitFactory default), POST with empty `Recaptcha` field succeeds (already implicitly covered by Task 1 but add an explicit `[Fact]` that names the scenario so the test matrix is documented).
  </behavior>
  <action>
    Edit `src/PassReset.Tests/Web/Controllers/RateLimitAndRecaptchaTests.cs` (append to existing file from Task 1):

    1. Add a second nested factory subclass `public sealed class RecaptchaEnabledFactory : WebApplicationFactory<Program>`:
       - `builder.UseEnvironment("Development");`
       - Same config keys as RateLimitFactory EXCEPT override:
         - `["ClientSettings:Recaptcha:Enabled"] = "true"`
         - `["ClientSettings:Recaptcha:PrivateKey"] = "6LeIxAcTAAAAAGG-vFI1TnRWxMZNFuojJ4WifJWe"` (NOTE: this is publicly documented by Google as an always-fail test key — see https://developers.google.com/recaptcha/docs/faq#id-like-to-run-automated-tests-with-recaptcha. Use this to keep test deterministic and documentable.)
         - `["ClientSettings:Recaptcha:FailOpenOnUnavailable"] = "false"` (make sure we get InvalidCaptcha, not silent pass)
       - Add XML comment at class level: `/// <summary>STAB-014(c) D-19: reCAPTCHA-enabled path. Hits real Google siteverify with an invalid token — see 09-CONTEXT.md §"D-19".</summary>`
    2. `[Fact] public async Task Recaptcha_EnabledWithInvalidToken_ReturnsInvalidCaptcha()`:
       - `using var factory = new RecaptchaEnabledFactory();`
       - `using var client = factory.CreateClient(...);`
       - Build request: `var req = MakeRequest("alice"); req = req with { Recaptcha = "ThisTokenWillNotVerify" };` — if `ChangePasswordModel` is a class not record, construct via property setter: `var req = MakeRequest("alice"); req.Recaptcha = "ThisTokenWillNotVerify";` (the existing MakeRequest sets `Recaptcha = string.Empty` — override to non-empty so it passes the "empty-token is silently handled" short-circuit and actually hits the Google call).
       - POST and assert: `Assert.Equal(HttpStatusCode.BadRequest, response.StatusCode);`
       - `var result = await ReadResultAsync(response); Assert.Contains(result!.Errors, e => e.ErrorCode == ApiErrorCode.InvalidCaptcha);`
       - Add `[Trait("Category", "RequiresInternet")]` attribute so CI can optionally skip if outbound HTTPS is unavailable — per D-19's note "CI must allow outbound https to google.com". This trait is advisory; do not add a `[SkippableFact]` conditional.
    3. `[Fact] public async Task Recaptcha_DisabledWithEmptyToken_RequestProceeds()`:
       - `using var factory = new RateLimitFactory();` (Recaptcha:Enabled=false already)
       - `using var client = factory.CreateClient(...);`
       - POST once with `MakeRequest("alice")` (empty Recaptcha field — RateLimitFactory's config means the path is skipped entirely).
       - `Assert.Equal(HttpStatusCode.OK, response.StatusCode);` (success — debug provider + no magic username + no recaptcha check).
    4. Do NOT add an `IRecaptchaVerifier` abstraction (D-19 explicit rejection).
    5. Do NOT modify `PasswordController`, `Program.cs`, or any production code.
    6. Do NOT mock `_recaptchaHttp` via reflection.
  </action>
  <verify>
    <automated>dotnet test src/PassReset.sln --configuration Release --filter "FullyQualifiedName~RateLimitAndRecaptchaTests.Recaptcha" --nologo</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Recaptcha_EnabledWithInvalidToken_ReturnsInvalidCaptcha" src/PassReset.Tests/Web/Controllers/RateLimitAndRecaptchaTests.cs` == 1
    - `grep -c "Recaptcha_DisabledWithEmptyToken_RequestProceeds" src/PassReset.Tests/Web/Controllers/RateLimitAndRecaptchaTests.cs` == 1
    - `grep -c "RecaptchaEnabledFactory" src/PassReset.Tests/Web/Controllers/RateLimitAndRecaptchaTests.cs` >= 2 (class + instance use)
    - `grep -n "Recaptcha:Enabled.*true" src/PassReset.Tests/Web/Controllers/RateLimitAndRecaptchaTests.cs` returns at least 1 match
    - `grep -n "Recaptcha:PrivateKey" src/PassReset.Tests/Web/Controllers/RateLimitAndRecaptchaTests.cs` returns at least 1 match
    - `grep -n "ApiErrorCode.InvalidCaptcha" src/PassReset.Tests/Web/Controllers/RateLimitAndRecaptchaTests.cs` returns at least 1 match
    - `grep -n "RequiresInternet" src/PassReset.Tests/Web/Controllers/RateLimitAndRecaptchaTests.cs` returns at least 1 match
    - Tests green: `dotnet test --filter "FullyQualifiedName~RateLimitAndRecaptchaTests.Recaptcha"` exits 0 with 2 passed
    - No production-code files modified: `git status --porcelain src/PassReset.Web/` shows no changes
  </acceptance_criteria>
  <done>Four-scenario STAB-014 coverage complete: 2 rate-limit + 2 recaptcha tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → /api/password | Unauthenticated POST; credential-stuffing + bot targets |
| Test Host → Google siteverify | Live outbound HTTPS to `google.com/recaptcha/api/siteverify` (D-19) — must be allowed by CI egress |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-02 | Denial of service / abuse | POST /api/password rate-limiter | mitigate (test coverage) | Integration test proves fixed-window policy returns 429 on 6th request; regression guard against accidental policy removal. |
| T-9-02b | Spoofing (bot) | reCAPTCHA v3 verification branch | mitigate (test coverage) | Test proves InvalidCaptcha returned when token fails Google siteverify; separate test proves disabled path doesn't accidentally gate everything with 400s. |
| T-9-02c | Test-isolation leak | Shared rate-limiter in-memory partition | mitigate | Per-test `WebApplicationFactory` disposal (Pitfall 1) — fresh partition per test; regression guard via explicit "fresh factory → first request 200" test. |
</threat_model>

<verification>
- `dotnet test src/PassReset.sln --configuration Release --filter "FullyQualifiedName~RateLimitAndRecaptchaTests" --nologo` exits 0 with 4 passed
- Full suite still green: `dotnet test src/PassReset.sln --configuration Release`
- If CI flakes on the reCAPTCHA network test, check outbound egress to `google.com:443` — D-19 explicitly accepts this dependency; do NOT introduce abstractions.
</verification>

<success_criteria>
STAB-014 complete when:
- 4 [Fact] tests exist — 2 rate-limit (enforce + isolation), 2 reCAPTCHA (enabled-reject + disabled-accept)
- All 4 tests pass against a clean build
- No production-code changes
- Per-test factory pattern preserved (rate-limit partition isolation)
</success_criteria>

<output>
After completion, create `.planning/phases/09-security-hardening/09-02-SUMMARY.md`.
</output>
