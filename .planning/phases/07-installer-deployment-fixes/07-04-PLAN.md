---
phase: 07-installer-deployment-fixes
plan: 04
type: execute
wave: 3
depends_on: [03]
files_modified:
  - deploy/Install-PassReset.ps1
  - docs/IIS-Setup.md
  - CHANGELOG.md
autonomous: false
requirements: [STAB-002, STAB-003]
requirements_addressed: [STAB-002, STAB-003]
touches_appsettings: false
rationale: >
  STAB-002 (same-version reconfigure prompt) modifies the upgrade-detection block at
  Install-PassReset.ps1:202-243. STAB-003 (AppPool identity read fix) modifies the BUG-003
  block at Install-PassReset.ps1:302-356. Both regions are in the upgrade-handling area of
  the same file; sequencing them in one plan avoids merge conflicts. depends_on plan 03 which
  also modifies Install-PassReset.ps1 in different regions — Wave 3 runs after Wave 2 completes.
  This plan also bundles docs/IIS-Setup.md update (STAB-006 follow-up from plan 03) and
  CHANGELOG.md entries for all six STAB-001..006 items.

  Per phase_specific_guidance, planner picks ONE STAB-003 fix from the three candidates in
  PATTERNS.md. Selected: candidate 2 (`Get-WebConfigurationProperty`). Rationale: most
  reliable across PS 5.1 / 7.x per PATTERNS.md note "more reliable across PS5.1/7"; it does
  not depend on provider warm-up timing (candidate 3) and does not depend on subtle
  PS-version-dependent behavior of `.Value` access (candidate 1). The decorator-style "warm-up
  then read" of candidate 3 is fragile in scripted contexts.

must_haves:
  truths:
    - "Re-running the installer with the current installed version prompts 'Re-configure existing installation?', not 'upgrade'"
    - "On reconfirm acknowledged, robocopy /MIR file mirror is skipped (no file changes), but app-pool/binding/config logic still runs"
    - "Upgrade preserves the existing AppPool identity without warning + fallback when AppPool exists with valid identity (SpecificUser, ApplicationPoolIdentity, NetworkService, LocalService, LocalSystem)"
    - "CHANGELOG.md [Unreleased] (or [1.4.0]) section contains one line per STAB-001..006 referencing the gh#NN issue"
    - "docs/IIS-Setup.md documents the new auto-install DISM flow as primary and manual remediation as fallback"
  artifacts:
    - path: "deploy/Install-PassReset.ps1"
      provides: "Reconfigure branch in upgrade-detection block + fixed AppPool identity read"
      contains: "Re-configure existing installation"
    - path: "docs/IIS-Setup.md"
      provides: "Updated dependency-install section reflecting STAB-006 auto-install path"
      contains: "DISM"
    - path: "CHANGELOG.md"
      provides: "Six entries for STAB-001..006 under [Unreleased] (or [1.4.0])"
      contains: "STAB-001"
  key_links:
    - from: "Install-PassReset.ps1 upgrade-detection block"
      to: "$isReconfigure flag"
      via: "version-equality branch sets flag, robocopy block gates on flag"
      pattern: "isReconfigure"
    - from: "Install-PassReset.ps1 BUG-003 AppPool block"
      to: "Get-WebConfigurationProperty"
      via: "replaces Get-ItemProperty | .Value"
      pattern: "Get-WebConfigurationProperty.*processModel"
---

<objective>
Three changes in one plan, all in the upgrade-handling region of `deploy/Install-PassReset.ps1`
plus docs/CHANGELOG bookkeeping:

1. **STAB-002 (gh#20) same-version prompt** — extend the upgrade-detection block at lines 202-243
   to add a version-equality branch. When `$parsedIncoming -eq $parsedCurrent`, prompt
   "Re-configure existing installation? [Y/N]". On confirm, set `$isReconfigure` flag and
   skip the robocopy /MIR mirror at lines 290-296.

2. **STAB-003 (gh#23) AppPool identity read fix** — replace the failing
   `Get-ItemProperty ... .Value` access at lines 306-315 with `Get-WebConfigurationProperty`
   (planner-selected candidate 2 from PATTERNS.md). Acceptance gate: warning + fallback at
   line 313 must NEVER fire when AppPool exists with valid identity.

3. **Docs + CHANGELOG (STAB-001..006 finalization)** — update `docs/IIS-Setup.md` to reflect
   the new DISM auto-install path (from plan 03) and add CHANGELOG entries for all six
   STAB items.

Purpose: Closes ROADMAP.md Phase 7 Success Criteria #2 and #3 (gh#20, gh#23). Finalizes
documentation and release-notes coverage for the entire phase.

Output: Final reconfigure branch + fixed AppPool read in `Install-PassReset.ps1`; updated
`docs/IIS-Setup.md`; six new CHANGELOG entries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-installer-deployment-fixes/07-CONTEXT.md
@.planning/phases/07-installer-deployment-fixes/07-PATTERNS.md
@deploy/Install-PassReset.ps1
@docs/IIS-Setup.md
@CHANGELOG.md
@.planning/milestones/v1.2.3-REQUIREMENTS.md

<interfaces>
<!-- Existing upgrade-detection branching at lines 202-243 — to be extended -->
```powershell
if ([version]::TryParse($currentVersion, [ref]$parsedCurrent) -and
    [version]::TryParse($incomingVersion, [ref]$parsedIncoming)) {
    if ($parsedIncoming -lt $parsedCurrent) { $isDowngrade = $true }
    # NEW: elseif ($parsedIncoming -eq $parsedCurrent) { $isReconfigure = $true }
}
```

<!-- BUG-003 broken read at lines 306-315 — to be replaced -->
```powershell
$existingIdentityType = (Get-ItemProperty "IIS:\AppPools\$AppPoolName" -Name processModel.identityType -ErrorAction Stop).Value
```
↓ replaced with ↓
```powershell
$existingIdentityType = (Get-WebConfigurationProperty -PSPath 'IIS:\' `
    -Filter "system.applicationHost/applicationPools/add[@name='$AppPoolName']" `
    -Name processModel.identityType -ErrorAction Stop).Value
```

<!-- Robocopy /MIR file mirror at lines 290-296 (per PATTERNS.md §STAB-002) -->
<!-- Currently invoked unconditionally on the upgrade path; gate on -not $isReconfigure -->

<!-- BUG-003 contract reference -->
<!-- See .planning/milestones/v1.2.3-REQUIREMENTS.md §BUG-003: AppPool identity preservation MUST not regress -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: STAB-002 — add version-equality reconfigure branch + skip robocopy mirror on reconfigure</name>
  <files>deploy/Install-PassReset.ps1</files>
  <read_first>
    - deploy/Install-PassReset.ps1 (post-plan-03 state; must see lines 202-243 and 290-296)
    - .planning/phases/07-installer-deployment-fixes/07-PATTERNS.md §STAB-002 (lines 56-74)
    - .planning/phases/07-installer-deployment-fixes/07-CONTEXT.md (Claude's discretion: STAB-002)
  </read_first>
  <action>
    1. Locate the version-comparison block at `Install-PassReset.ps1:202-217`. Inside the
       `if ([version]::TryParse(...))` block, ADD an `elseif` branch BEFORE/after the existing
       `$isDowngrade` branch:
       ```powershell
       if ([version]::TryParse($currentVersion, [ref]$parsedCurrent) -and
           [version]::TryParse($incomingVersion, [ref]$parsedIncoming)) {
           if     ($parsedIncoming -lt $parsedCurrent) { $isDowngrade   = $true }
           elseif ($parsedIncoming -eq $parsedCurrent) { $isReconfigure = $true }
       }
       ```
       Initialize `$isReconfigure = $false` near the existing `$isDowngrade = $false` declaration
       (find via `grep -n "isDowngrade" deploy/Install-PassReset.ps1`).
    2. In the upgrade-confirmation block at lines 230-243, ADD a parallel reconfigure path. The
       existing block looks like:
       ```powershell
       if ($isUpgrade -or $isDowngrade) {
           # ...existing upgrade/downgrade prompt...
       }
       ```
       Add the reconfigure prompt with the EXACT wording per CONTEXT (D-Claude's discretion on STAB-002):
       ```powershell
       if ($isReconfigure) {
           Write-Step "Existing installation v$currentVersion is the same version. This will RE-CONFIGURE (not upgrade)."
           if (-not $Force) {
               $confirm = Read-Host '  Re-configure existing installation? [Y/N]'
               if ($confirm -notmatch '^[Yy]') {
                   Write-Host "`n  Cancelled." -ForegroundColor Yellow; exit 0
               }
           } else {
               Write-Ok '-Force specified — re-configuring without file mirror'
           }
       }
       ```
    3. Locate the robocopy /MIR file mirror block at lines 290-296 (per PATTERNS.md §STAB-002).
       Wrap (or gate) the robocopy invocation:
       ```powershell
       if (-not $isReconfigure) {
           # existing robocopy /MIR call
       } else {
           Write-Ok 'Reconfigure mode — skipping file mirror; existing publish folder preserved'
       }
       ```
    4. CONFIRM: app-pool / binding / config logic that follows the robocopy block must STILL execute
       on reconfigure (per PATTERNS.md §STAB-002 — "skip the robocopy /MIR mirror but still re-run
       app-pool / binding / config logic"). DO NOT wrap downstream blocks in `-not $isReconfigure`.
       Only the robocopy /MIR block is gated.
    5. The new prompt must NOT use the word "upgrade" — verify with `grep -n "Re-configure" deploy/Install-PassReset.ps1`
       and confirm no "upgrade" wording in the same prompt context.
  </action>
  <verify>
    <automated>
      powershell.exe -NoProfile -Command "
        $errs=$null;
        [System.Management.Automation.Language.Parser]::ParseFile('deploy/Install-PassReset.ps1', [ref]$null, [ref]$errs) | Out-Null;
        if ($errs) { $errs | Out-String; exit 1 };
        $c = Get-Content deploy/Install-PassReset.ps1 -Raw;
        if ($c -notmatch '\\$isReconfigure\\s*=\\s*\\$true') { throw 'Missing isReconfigure flag set' };
        if ($c -notmatch 'Re-configure existing installation') { throw 'Missing reconfigure prompt text' };
        if ($c -notmatch '-not\\s+\\$isReconfigure') { throw 'Robocopy block not gated on reconfigure flag' };
        Write-Host 'OK'
      "
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE '\\$parsedIncoming\\s+-eq\\s+\\$parsedCurrent' deploy/Install-PassReset.ps1` returns one match (the new equality branch).
    - `grep -n '\\$isReconfigure' deploy/Install-PassReset.ps1` returns at least three matches: declaration init `= $false`, branch assignment `= $true`, robocopy gate `-not $isReconfigure`.
    - `grep -n 'Re-configure existing installation' deploy/Install-PassReset.ps1` returns one match.
    - `grep -nE 'Read-Host.*[Uu]pgrade' deploy/Install-PassReset.ps1` does NOT match a NEW prompt next to the reconfigure flag (the existing upgrade prompt is OK; the reconfigure prompt must not say "upgrade").
    - The robocopy /MIR call is wrapped with `if (-not $isReconfigure) { ... }`.
    - Parser::ParseFile zero errors on PS 5.1.
  </acceptance_criteria>
  <done>Version-equality branch added, reconfigure prompt uses correct wording, robocopy gated. Validates ROADMAP.md Phase 7 Success Criterion #2.</done>
</task>

<task type="auto">
  <name>Task 2: STAB-003 — replace failing AppPool identity read with Get-WebConfigurationProperty</name>
  <files>deploy/Install-PassReset.ps1</files>
  <read_first>
    - deploy/Install-PassReset.ps1 (post-Task-1 state; must see lines 302-356)
    - .planning/phases/07-installer-deployment-fixes/07-PATTERNS.md §STAB-003 (lines 77-102)
    - .planning/phases/07-installer-deployment-fixes/07-CONTEXT.md (Claude's discretion: STAB-003)
    - .planning/milestones/v1.2.3-REQUIREMENTS.md §BUG-003 (the four-branch preserve contract this fix is RESTORING)
  </read_first>
  <action>
    1. Locate the BUG-003 block at `Install-PassReset.ps1:302-356`. The broken read is at
       lines 306-315 (per PATTERNS.md):
       ```powershell
       if ($poolExists) {
           try {
               $existingIdentityType = (Get-ItemProperty "IIS:\AppPools\$AppPoolName" -Name processModel.identityType -ErrorAction Stop).Value
               if ($existingIdentityType -eq 'SpecificUser' -or $existingIdentityType -eq 3) {
                   $existingIdentity = (Get-ItemProperty "IIS:\AppPools\$AppPoolName" -Name processModel.userName -ErrorAction Stop).Value
               }
           } catch {
               Write-Warning "Could not read existing AppPool identity: $($_.Exception.Message). Will fall through to default handling."
           }
       }
       ```

    2. Replace BOTH `Get-ItemProperty | .Value` reads with `Get-WebConfigurationProperty` (planner
       selection per phase_specific_guidance — candidate 2 from PATTERNS.md, most reliable across
       PS 5.1 / 7.x):
       ```powershell
       if ($poolExists) {
           try {
               $appPoolFilter = "system.applicationHost/applicationPools/add[@name='$AppPoolName']"
               $existingIdentityType = (Get-WebConfigurationProperty -PSPath 'IIS:\' `
                   -Filter $appPoolFilter -Name processModel.identityType -ErrorAction Stop).Value
               if ($existingIdentityType -eq 'SpecificUser' -or $existingIdentityType -eq 3) {
                   $existingIdentity = (Get-WebConfigurationProperty -PSPath 'IIS:\' `
                       -Filter $appPoolFilter -Name processModel.userName -ErrorAction Stop).Value
               }
           } catch {
               Write-Warning "Could not read existing AppPool identity: $($_.Exception.Message). Will fall through to default handling."
           }
       }
       ```

    3. DO NOT modify the four-branch preserve logic at lines 330-356 (per PATTERNS.md §STAB-003
       acceptance gate — "DO NOT redesign the four-branch block at lines 330-356. Only fix the read.").
       The branches that handle `SpecificUser`, `ApplicationPoolIdentity`, `NetworkService`,
       `LocalService`, `LocalSystem` remain byte-identical.

    4. The Write-Warning fallback message at line 313 (or wherever the catch block lives) is the
       canary: per acceptance gate, this message MUST NOT fire when AppPool exists with a valid
       identity. Task 3 UAT verifies this.
  </action>
  <verify>
    <automated>
      powershell.exe -NoProfile -Command "
        $errs=$null;
        [System.Management.Automation.Language.Parser]::ParseFile('deploy/Install-PassReset.ps1', [ref]$null, [ref]$errs) | Out-Null;
        if ($errs) { $errs | Out-String; exit 1 };
        $c = Get-Content deploy/Install-PassReset.ps1 -Raw;
        if ($c -match 'Get-ItemProperty\s+\"IIS:\\\\AppPools\\\\\\\$AppPoolName\"\s+-Name\s+processModel') {
          throw 'Old Get-ItemProperty read still present'
        };
        if ($c -notmatch 'Get-WebConfigurationProperty.*processModel\.identityType') {
          throw 'Get-WebConfigurationProperty replacement not present for identityType'
        };
        if ($c -notmatch 'Get-WebConfigurationProperty.*processModel\.userName') {
          throw 'Get-WebConfigurationProperty replacement not present for userName'
        };
        Write-Host 'OK'
      "
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'Get-ItemProperty.*IIS:\\\\AppPools.*processModel' deploy/Install-PassReset.ps1` returns ZERO matches (old broken read removed).
    - `grep -nE 'Get-WebConfigurationProperty.*processModel\\.identityType' deploy/Install-PassReset.ps1` returns one match.
    - `grep -nE 'Get-WebConfigurationProperty.*processModel\\.userName' deploy/Install-PassReset.ps1` returns one match.
    - The four-branch preserve block (around lines 330-356) is byte-identical to pre-change (verify via `git diff -U0 deploy/Install-PassReset.ps1` — the only diff in the BUG-003 region should be the two `Get-ItemProperty` → `Get-WebConfigurationProperty` substitutions and the new `$appPoolFilter` variable).
    - Parser::ParseFile zero errors on PS 5.1.
  </acceptance_criteria>
  <done>AppPool identity read uses Get-WebConfigurationProperty; four-branch preserve logic unchanged. Validates ROADMAP.md Phase 7 Success Criterion #3.</done>
</task>

<task type="auto">
  <name>Task 3: Update docs/IIS-Setup.md with new auto-install DISM flow + manual fallback</name>
  <files>docs/IIS-Setup.md</files>
  <read_first>
    - docs/IIS-Setup.md (full file)
    - .planning/phases/07-installer-deployment-fixes/07-PATTERNS.md §docs/IIS-Setup.md (lines 237-244)
    - deploy/Install-PassReset.ps1 (post-plan-03 dependency block, for the canonical feature list)
  </read_first>
  <action>
    1. Open `docs/IIS-Setup.md` and locate the IIS roles / dependency-install section.
    2. Add a new subsection (or update the existing one) titled e.g. `### Automatic dependency install (recommended)`:
       ```markdown
       Run `Install-PassReset.ps1` from an elevated PowerShell. The installer detects missing
       IIS roles and offers a single Y/N prompt to install them via DISM. Choose **Y** to enable
       all missing features in one step:

       - Web-Server, Web-WebServer, Web-Static-Content, Web-Default-Doc
       - Web-Http-Errors, Web-Http-Logging, Web-Filtering, Web-Mgmt-Console
       - Web-Asp-Net45, Web-Net-Ext45

       Use `-Force` for unattended/CI installs — DISM enable runs without prompting.

       The .NET 10 Hosting Bundle is **not** auto-installed. If missing, the installer prints
       the download URL and exits cleanly. Download from
       <https://dotnet.microsoft.com/download/dotnet/10.0> ("ASP.NET Core Runtime - Hosting Bundle"),
       then re-run the installer.
       ```
    3. Move the existing manual `Install-WindowsFeature` / Server Manager instructions UNDER a new
       `### Manual fallback (declined Y/N prompt or pre-Windows-Server-2019)` heading. Keep the
       existing content verbatim — only the section heading and ordering change.
    4. Confirm the document still compiles (no broken markdown, internal anchors still resolve).
       Spot-check by scanning for `](#` links to renamed sections.
    5. The list of features above must EXACTLY match the `$requiredFeatures` array in
       `Install-PassReset.ps1` post-plan-03. If plan 03 added anything beyond the documented set,
       update this list to match (read the script first to confirm).
  </action>
  <verify>
    <automated>
      powershell.exe -NoProfile -Command "
        $c = Get-Content docs/IIS-Setup.md -Raw;
        if ($c -notmatch 'DISM') { throw 'Missing DISM mention' };
        if ($c -notmatch 'Web-Asp-Net45') { throw 'Missing Web-Asp-Net45 in docs' };
        if ($c -notmatch 'dotnet\.microsoft\.com/download/dotnet/10\.0') { throw 'Missing Hosting Bundle URL' };
        if ($c -notmatch '(?i)manual\s+fallback') { throw 'Missing manual fallback section' };
        Write-Host 'OK'
      "
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "DISM" docs/IIS-Setup.md` returns at least one match.
    - `grep -n "Web-Asp-Net45" docs/IIS-Setup.md` and `grep -n "Web-Net-Ext45"` each return at least one match.
    - `grep -n "dotnet.microsoft.com/download/dotnet/10.0" docs/IIS-Setup.md` returns one match.
    - `grep -inE "manual\\s+fallback" docs/IIS-Setup.md` returns at least one match (heading or paragraph indicator).
    - Feature list in docs matches the `$requiredFeatures` union in `Install-PassReset.ps1` (cross-check by listing both, no missing entries either direction).
  </acceptance_criteria>
  <done>docs/IIS-Setup.md documents the new DISM auto-install primary path and preserves the manual fallback. STAB-006 documentation closed.</done>
</task>

<task type="auto">
  <name>Task 4: Add CHANGELOG.md entries for STAB-001..006 under [Unreleased]</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (full file; observe heading levels and entry formatting from prior [1.3.x] sections)
    - .planning/REQUIREMENTS.md §v1.4.0 (line numbers 19-24 — STAB-001..006 with gh#NN refs)
    - .planning/phases/07-installer-deployment-fixes/07-PATTERNS.md §CHANGELOG.md (lines 255-258)
    - CLAUDE.md §"Commit convention" (for scope tags: installer, provider, deploy, docs)
  </read_first>
  <action>
    1. Open `CHANGELOG.md`. Locate the `## [Unreleased]` section. If a `## [1.4.0] — YYYY-MM-DD`
       section already exists, add entries there instead. If neither exists, create a fresh
       `## [Unreleased]` heading directly under the document title.
    2. Add the following six bullets under a `### Fixed` subsection (mirroring existing entry style):
       ```markdown
       ### Fixed

       - **STAB-001 (gh#19):** Fresh install no longer fails when IIS Default Web Site is bound
         to port 80. Installer detects the conflict and offers stop / alternate-port / abort
         (interactive); `-Force` mode picks an alternate port from 8080-8090. The final reachable
         URL is printed at install completion. *(installer)*
       - **STAB-002 (gh#20):** Re-running the installer with the currently installed version
         now prompts "Re-configure existing installation?" instead of "upgrade". On confirm the
         file mirror is skipped; app-pool / binding / config logic still re-runs. *(installer)*
       - **STAB-003 (gh#23):** Upgrade no longer prints a spurious "Could not read existing
         AppPool identity" warning before falling back to defaults. AppPool identity is now read
         via `Get-WebConfigurationProperty`, which is reliable across Windows PowerShell 5.1
         and PowerShell 7.x. Restores the BUG-003 preserve contract. *(installer)*
       - **STAB-004 (gh#36):** Two consecutive password changes for the same user no longer
         raise `UnauthorizedAccessException` (`E_ACCESSDENIED`). A new pre-check compares
         `pwdLastSet` against the domain `minPwdAge` and returns
         `ApiErrorCode.PasswordTooRecentlyChanged` (19) with a minute-level remaining-time
         message. The existing COMException catch remains as a defense-in-depth floor. *(provider)*
       - **STAB-005 (gh#39):** `Uninstall-PassReset.ps1` now parses cleanly on Windows
         PowerShell 5.1 and PowerShell 7.x. Re-saved as UTF-8 with BOM; Unicode `─` (U+2500)
         box-drawing dividers replaced with ASCII `---`. `-KeepFiles` continues to honor the
         publish folder. *(deploy)*
       - **STAB-006 (gh#21):** `Install-PassReset.ps1` detects missing IIS roles
         (`Web-Server`, `Web-Asp-Net45`, `Web-Net-Ext45`, plus the existing must-have set)
         and offers a single Y/N consent prompt to install all missing features via DISM
         (gated by `ShouldProcess` so `-WhatIf` works). A missing .NET 10 Hosting Bundle
         prints the download URL and exits 0 cleanly (no auto-install). *(installer)*
       ```
    3. Maintain the existing CHANGELOG style (heading levels, blank-line spacing, scope tags
       in italics matching `*(installer)*` / `*(provider)*` / `*(deploy)*` per CLAUDE.md
       commit-convention scopes). If the existing CHANGELOG uses a different style, conform
       to that style — bullet content above is the truth, formatting follows existing precedent.
    4. DO NOT modify any prior `[1.3.x]` section. Append-only edits.
  </action>
  <verify>
    <automated>
      powershell.exe -NoProfile -Command "
        $c = Get-Content CHANGELOG.md -Raw;
        foreach ($id in @('STAB-001','STAB-002','STAB-003','STAB-004','STAB-005','STAB-006')) {
          if ($c -notmatch [regex]::Escape($id)) { throw \"Missing entry for $id\" }
        };
        foreach ($gh in @('gh#19','gh#20','gh#21','gh#23','gh#36','gh#39')) {
          if ($c -notmatch [regex]::Escape($gh)) { throw \"Missing reference to $gh\" }
        };
        Write-Host 'OK'
      "
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'STAB-00[1-6]' CHANGELOG.md` returns at least 6 matches (one per ID; more is OK if cross-referenced elsewhere).
    - `grep -nE 'gh#(19|20|21|23|36|39)' CHANGELOG.md` returns at least 6 matches.
    - All six entries appear under a single `## [Unreleased]` (or `## [1.4.0]`) section, NOT scattered across prior versions.
    - Prior `[1.3.x]` sections show zero diff (`git diff CHANGELOG.md` only adds lines under the v1.4.0 / Unreleased heading).
  </acceptance_criteria>
  <done>Six CHANGELOG entries present with STAB-NNN + gh#NN references. Phase release-notes coverage complete.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Operator UAT — same-version reconfigure + AppPool identity preservation</name>
  <files>n/a — operator UAT, no files modified by this task</files>
  <action>Execute the verification steps in <how-to-verify>; capture transcripts; reply with resume-signal.</action>
  <verify><automated>MISSING — manual operator UAT, see &lt;how-to-verify&gt; below</automated></verify>
  <done>Operator replies "approved" with transcript excerpts proving each scenario per <how-to-verify>.</done>
  <what-built>
    STAB-002 reconfigure branch: same version → "Re-configure existing installation?" prompt,
    file mirror skipped on confirm. STAB-003 AppPool read fixed via Get-WebConfigurationProperty
    — warning + fallback no longer fires when AppPool exists with valid identity.
    Docs and CHANGELOG updated.
  </what-built>
  <how-to-verify>
    On the same test box used for plan 07-03 UAT:

    Scenario A — Same-version reconfigure (interactive):
    1. Confirm PassReset is installed at version `vX.Y.Z` (whatever was installed last).
    2. Re-build (or use the same package): `.\Publish-PassReset.ps1 -Version vX.Y.Z` → unzip same version.
    3. Elevated `powershell.exe`: `.\Install-PassReset.ps1`
    4. Expected prompt: `Re-configure existing installation? [Y/N]`. The word "upgrade" must NOT appear in this prompt.
    5. Choose Y. Expected: `Reconfigure mode — skipping file mirror; existing publish folder preserved` is logged. App-pool / binding / config logic still runs. Site remains reachable.

    Scenario B — Same-version reconfigure (-Force):
    1. Same setup as A. Run `.\Install-PassReset.ps1 -Force <other params>`.
    2. Expected: no prompt. `-Force specified — re-configuring without file mirror` is logged.

    Scenario C — Upgrade preserves AppPool identity (STAB-003 regression):
    1. Configure the existing PassReset AppPool with `SpecificUser` identity (e.g., `DOMAIN\svc-passreset`) via IIS Manager.
    2. Build a higher-version package (e.g., bump csproj to `vX.Y.Z+1`): `.\Publish-PassReset.ps1 -Version vX.Y.Z+1`.
    3. Elevated: `.\Install-PassReset.ps1`. Expected: upgrade path runs.
    4. Verify: console output does NOT contain `Could not read existing AppPool identity` (the BUG-003 warning canary).
    5. Verify post-install: AppPool identity is still `DOMAIN\svc-passreset` (not reverted to ApplicationPoolIdentity).
       Check via: `Get-WebConfigurationProperty -PSPath 'IIS:\' -Filter "system.applicationHost/applicationPools/add[@name='PassReset']" -Name processModel.userName`

    Capture transcripts and the AppPool identity verification output. Paste into resume comment.
  </how-to-verify>
  <resume-signal>Reply "approved" with transcript excerpts proving each scenario, or describe failures with exact text.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator shell → IIS configuration | Reconfigure path may re-set bindings without overwriting files; AppPool read uses webadministration provider |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-11 | Tampering | Reconfigure skipping file mirror could mask a partial-deploy state | accept | Reconfigure path explicitly preserves the existing publish folder. Operator chose this path knowingly via Y/N prompt. If the publish folder is corrupted, the operator can re-run with a higher version (forces upgrade path with mirror) or run `Uninstall-PassReset.ps1` first. |
| T-07-12 | Information Disclosure | AppPool identity username appears in console output | accept | Username (e.g., `DOMAIN\svc-passreset`) was already printed by the existing four-branch preserve logic before this fix; no new exposure. Password is never read or printed. |
| T-07-13 | Spoofing | Reconfigure path running on a stale install (operator forgot they had something custom) | accept | Reconfigure prompt clearly states the version and the word "RE-CONFIGURE". `-Force` mode logs `re-configuring without file mirror`. No silent action. |
| T-07-14 | Denial of Service | Get-WebConfigurationProperty fails on a corrupt applicationHost.config | mitigate | The catch block at the existing line 313 still catches the failure and falls through to default handling (four-branch logic at :330-356 unchanged). Behavior degrades to pre-BUG-003 state, not worse. |
</threat_model>

<verification>
1. Parser::ParseFile zero errors on PS 5.1 after both Task 1 and Task 2 edits.
2. `$isReconfigure` flag declared, set, and consumed (gates robocopy mirror only).
3. Reconfigure prompt does NOT contain the word "upgrade".
4. `Get-WebConfigurationProperty` replaces `Get-ItemProperty | .Value` for both identityType and userName reads.
5. Four-branch preserve logic at :330-356 is unchanged.
6. CHANGELOG has all six STAB-001..006 + gh#NN entries.
7. docs/IIS-Setup.md mentions DISM, Web-Asp-Net45, Web-Net-Ext45, Hosting Bundle URL, manual fallback.
8. Operator UAT (Task 5) — three live scenarios verifying reconfigure prompt and AppPool preservation.
</verification>

<success_criteria>
ROADMAP.md Phase 7 Success Criterion #2: Re-running the installer with the current installed version prompts "re-configure", not "upgrade" (gh#20).
ROADMAP.md Phase 7 Success Criterion #3: Upgrade preserves the existing AppPool identity without warning + fallback (gh#23).
Plus phase-level closure: docs/IIS-Setup.md and CHANGELOG.md cover all six STAB items (#1-6).
</success_criteria>

<output>
After completion, create `.planning/phases/07-installer-deployment-fixes/07-04-SUMMARY.md`
documenting:
- Insertion point of `$isReconfigure` flag declaration and the equality branch line numbers
- Robocopy gate location and the gating expression used
- Before/after snippet of the AppPool identity read (one or two lines max)
- Confirmation that the four-branch preserve block at :330-356 has zero git diff outside the two read-line replacements
- List of CHANGELOG entries added (STAB-IDs + gh#NN refs)
- docs/IIS-Setup.md sections added/restructured
- Operator UAT transcript excerpts for scenarios A/B/C
</output>
